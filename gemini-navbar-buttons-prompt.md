# Gemini — top navbar buttons (match the existing set, light + dark in ONE image each)

Paste this with **one finished icon from the existing set attached** as the style reference. Work in **small batches (3–4 buttons at a time)** so it stays consistent and doesn't crash.

> Here's an icon from my existing LearnSomm set — same style, same line weight, same palette, do NOT restyle. I need a set of **top navigation bar buttons** in that exact style. These are pill-shaped buttons that sit in a horizontal toolbar: each has a small icon and a short text label on the pill.
>
> **For every button, put the LIGHT THEME version and the DARK THEME version in the SAME image**, side by side (light on the left, dark on the right), clearly the same button in both themes. Light theme = dark ink/garnet on warm parchment `#F4F2EA`; dark "cellar" theme = cream/gold on `#14100c`. Accent garnet `#6E1B2E`, antique gold `#b5862a`. Transparent background around the pills.
>
> **BUTTONS (one image each, light+dark together):**
> 1. **Current-Category template** — this is special: a blank/neutral pill that acts as a *holder*. Leave room for a **short category word** (e.g. "Wine", "Spirits") with a **small version of that category's sidebar icon** sitting next to it. Design it as an empty template that text + a small icon will populate over — show one example filled in ("Wine" + a small wine icon) and note it's a reusable holder.
> 2. **Search** — magnifier.
> 3. **Random Wheel** — dice or shuffle motif (it spins a random wheel).
> 4. **Cheat Sheet** — reference-card motif.
> 5. **Journal** — notebook/pen motif.
> 6. **Build a Challenge** — target/flag with a tiny solo/pair/group figure cue.
> 7. **Learning Quest** — a path/map or compass motif (the free study track).
> 8. **Pairing Mode** — IMPORTANT: this is a **technology / data pairing** concept (like syncing or connecting two devices side by side), **NOT** a food-and-beverage pairing. Use a tech motif — two panels/screens linked, a sync/link glyph, or paired nodes — so it reads as "connect/sync," not "wine & cheese."
> 9. **Design** — a palette/pen-and-ruler motif (design-your-wheel).
> 10. **Arcade** — a playful game motif (joystick/controller), still on-brand and restrained.
> 11. **Lightning Hour** — a lightning bolt; give a calm state and a glowing/"lit" state in the same image.
> 12. **Tip the Somm** — a generous/gratuity motif (coin into a glass, or a tip jar) — warm, not pushy.
> 13. **Focus** — an expand-to-fullscreen / "clear the room" glyph; make it read as a distinct, slightly larger button.
> 14. **Light/Dark switch** — make it ours: a wine glass filling with light vs. a candlelit cellar (the two states), not a generic sun/moon.
>
> Keep all pills the same height and style as each other. Match the reference icon's finish exactly — do not modernize or invent a new look.

**File names when they come back:** `nav-category-template.png`, `nav-search.png`, `nav-random.png`, `nav-cheat.png`, `nav-journal.png`, `nav-build-challenge.png`, `nav-quest.png`, `nav-pairing.png`, `nav-design.png`, `nav-arcade.png`, `nav-lightning.png`, `nav-tip.png`, `nav-focus.png`, `nav-theme-switch.png`. Each file already contains both themes. Bring them here and I'll mount them.
